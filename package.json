{
  "name": "node-elm-stdio",
  "version": "1.0.0",
  "description": "Pipe data to an Elm program as flags, pipe back the response",
  "main": "stdio",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "bin": {
    "elm-stdio": "./bin/elm-stdio"
  },
  "keywords": [
    "elm",
    "stdin",
    "stdout",
    "stdio",
    "pipe"
  ],
  "author": "Andreas Hultgren",
  "license": "ISC",
  "dependencies": {
    "argv": "0.0.2",
    "min-document": "^2.19.0"
  }
}
